{
  "name": "@kayodebristol/deno-gun",
  "version": "0.2020.1235",
  "description": "A Deno-compatible version of the GUN database ecosystem",
  "type": "module",
  "scripts": {
    "test": "deno test --no-check --allow-net --allow-read --allow-write --allow-env"
  }
}
